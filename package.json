{
  "name": "oneline2video",
  "private": true,
  "packageManager": "pnpm@10.8.1",
  "pnpm": {
    "onlyBuiltDependencies": ["@swc/core", "bcrypt", "electron", "esbuild"]
  },
  "scripts": {
    "dev": "pnpm --filter @orison/desktop-shell dev",
    "dev:server": "pnpm --filter @orison/server dev",
    "dev:agent": "pnpm --filter @orison/agent dev",
    "build": "turbo run build",
    "build:desktop": "pnpm --filter @orison/desktop-shell build",
    "build:server": "pnpm --filter @orison/server build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "jsdom": "^26.1.0",
    "turbo": "^2.0.12",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
