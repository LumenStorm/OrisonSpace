{
  "name": "@orison/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=.env.agent src/app.ts",
    "build": "tsup src/app.ts --format esm --out-dir dist --clean",
    "test": "vitest --run",
    "lint": "echo agent lint placeholder",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@orison/shared-contracts": "file:../../packages/shared-contracts",
    "@orison/story-sync": "workspace:*",
    "fastify": "^5.2.1",
    "fastify-plugin": "^5.0.1",
    "pino": "^9.7.0",
    "pino-pretty": "^13.0.0",
    "pino-roll": "^3.1.0",
    "yaml": "^2.8.1",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.19.3"
  }
}
