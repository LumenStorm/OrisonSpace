/**
 * Default service URLs used across desktop-ui, local-bff, and shell CSP.
 * Override via environment variables in production.
 */
export const DEFAULT_API_BASE = 'http://localhost:4000';
export const DEFAULT_AGENT_URL = 'http://localhost:18422';
