{
  "name": "@orison/shared-utils",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "lint": "echo shared-utils lint placeholder",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yaml": "^2.7.1"
  }
}
